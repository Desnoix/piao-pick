version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./strategies:/app/strategies
    environment:
      - DB_PATH=/app/data/piao_pick.db
    restart: unless-stopped
